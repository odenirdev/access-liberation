import styled from "styled-components";

export const Container = styled.tr`
  .access-approval-room,
  .access-approval-dh_access,
  .access-approval-button {
    text-align: center;
  }

  .access-approval-room,
  .access-approval-dh_access {
    white-space: nowrap;
    font-size: 0.7rem;

    svg {
      margin-right: 0.1rem;
    }
  }
`;
